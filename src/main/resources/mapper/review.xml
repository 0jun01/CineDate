<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tenco.movie.repository.interfaces.ReviewRepository">

<select id="findByMovieId" resultType="com.tenco.movie.repository.model.Review">
        select * from review_tb where movie_id = #{movie_id} order by review_date desc
    </select>

</mapper>