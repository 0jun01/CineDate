<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper
	namespace="com.tenco.movie.repository.interfaces.DateManagerRepocitory">

	<!-- 매치신청 기다리기 -->
	<insert id="movieSuggest">
		INSERT INTO matching_tb(s_user_id, r_user_id) VALUES
		( #{userId}, #{partNerId} )
	</insert>
	
	<update id="retryDate">
		update matching_tb set status = 0 WHERE s_user_id = #{userId} AND r_user_id = #{partNerId}
	</update>
	
	<update id="isOkDate">
		update matching_tb set status = 1 WHERE s_user_id = #{partNerId}  AND r_user_id = #{userId}
	</update>
	
	<update id="isRefuseDate">
		update matching_tb set status = 2 WHERE s_user_id = #{partNerId}  AND r_user_id = #{userId}
	</update>

	<select id="matchingList"
		resultType="com.tenco.movie.dto.matchingDTO">
		SELECT p.*,
		m.status,
		CASE
		WHEN m.s_user_id = #{principalId} THEN 1
		WHEN m.r_user_id = #{principalId} THEN 2
		END AS request_type
		FROM profile_tb p
		JOIN matching_tb m ON p.user_id = m.r_user_id OR p.user_id = m.s_user_id
		WHERE (m.s_user_id = #{principalId} OR m.r_user_id = #{principalId})
		AND p.user_id != #{principalId} AND m.status != 2
	</select>

	<select id="datailCount"
		resultType="com.tenco.movie.dto.detailCountDTO">
		select * from detail_count_tb WHERE user_id = #{userId}
	</select>

	<update id="updateOriginCount">
		UPDATE detail_count_tb SET origin_count = #{count}
		WHERE user_id = #{userId}
	</update>

	<update id="updateIpurchaseCount">
		UPDATE detail_count_tb SET purchase_count = #{count}
		WHERE user_id = #{userId}
	</update>

	<update id="resetAllDrtailCount">
		UPDATE detail_count_tb SET origin_count = 3
	</update>

	<select id="searchUserById"
		resultType="com.tenco.movie.repository.model.User">
		select * from user_tb WHERE id = #{id}
	</select>


</mapper>