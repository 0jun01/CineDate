<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.tenco.movie.repository.interfaces.ProfileRepository">

	<select id="searchProfile" resultType="com.tenco.movie.repository.model.DateProfile">
		SELECT * FROM profile_tb WHERE user_id = #{userId}
	</select>

	<insert id="createdProfile">
		INSERT INTO profile_tb (user_id,
		nick_name,
		introduce,
		first_origin_file_name,
		seocnd_origin_file_name,
		first_upload_file_name,
		second_upload_file_name
		) VALUES ( #{userId}, #{nickName}, #{introduce}, #{firstOriginFileName}, #{secondOriginFileName}, #{firstUploadFileName}, #{secondUploadFileName})
	</insert>
	
	<select id="searchNickName"  resultType="com.tenco.movie.repository.model.DateProfile">
		SELECT * FROM profile_tb WHERE nick_name = #{nickname}
	</select>
	

</mapper>